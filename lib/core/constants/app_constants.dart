class AppConstants {
  const AppConstants._();

  static const appName = 'FadeOut';
  static const brandName = 'HelaGen';
  static const packageName = 'com.helagen.fadeout';

  static const timerPresetsMinutes = [5, 10, 15, 30, 45, 60, 90];
  static const defaultTimerMinutes = 30;
  static const defaultFadeSeconds = 60;
  static const proLifetimePriceUsd = '5.99';

  static const iosMediaControlDisclaimer =
      'On iOS, media control depends on iOS and the currently playing app.';
}
